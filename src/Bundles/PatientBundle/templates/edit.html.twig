{% extends "layouts/base.html.twig" %}
{% import "components/forms.html.twig" as forms %}
{% import "components/ui.html.twig" as ui %}

{% block title %}{{ 'patient.edit.page_title'|trans }}{% endblock %}

{% block body %}
    <div class="ui segment">
        <h2 class="ui header">{{ 'patient.edit.title_full'|trans({'%first_name%': patient.first_name, '%last_name%': patient.last_name}) }}</h2>
        <div class="ui divider"></div>

        <form class="ui form" method="POST" action="/patients/edit?id={{ patient.id }}">
            <div class="three fields">
                {{ forms.input('last_name', 'patient.new.last_name'|trans, patient.last_name, 'text', 'patient.new.last_name_placeholder'|trans, errors.last_name ?? null, {'required': 'required'}) }}
                {{ forms.input('first_name', 'patient.new.first_name'|trans, patient.first_name, 'text', 'patient.new.first_name_placeholder'|trans, errors.first_name ?? null, {'required': 'required'}) }}
                {{ forms.input('middle_name', 'patient.new.middle_name'|trans, patient.middle_name, 'text', 'patient.new.middle_name_placeholder'|trans) }}
            </div>
            <div class="three fields">
                {{ forms.input('birth_date', 'patient.new.birth_date'|trans, patient.birth_date, 'date', '', errors.birth_date ?? null, {'required': 'required'}) }}
                {{ forms.select('gender', 'patient.new.gender'|trans, {'male': 'patient.new.genders.male'|trans, 'female': 'patient.new.genders.female'|trans, 'other': 'patient.new.genders.other'|trans}, patient.gender, errors.gender ?? null, {'required': 'required'}) }}
                {{ forms.input('phone', 'patient.new.phone'|trans, patient.phone, 'tel', 'patient.new.phone_placeholder'|trans, errors.phone ?? null, {'required': 'required'}) }}
            </div>
            <div class="two fields">
                {{ forms.input('email', 'patient.new.email'|trans, patient.email, 'email', 'patient.new.email_placeholder'|trans, errors.email ?? null) }}
                {{ forms.input('address', 'patient.new.address'|trans, patient.address, 'text', 'patient.new.address_placeholder'|trans) }}
            </div>
            <div class="three fields">
                {{ forms.input('tax_id', 'patient.new.tax_id'|trans, patient.tax_id, 'text', 'patient.new.tax_id_placeholder'|trans) }}
                {{ forms.input('document_id', 'patient.new.document_id'|trans, patient.document_id, 'text', 'patient.new.document_id_placeholder'|trans) }}
                {{ forms.select('marital_status', 'patient.new.marital_status'|trans, {'single': 'patient.new.marital_statuses.single'|trans, 'married': 'patient.new.marital_statuses.married'|trans, 'divorced': 'patient.new.marital_statuses.divorced'|trans, 'widowed': 'patient.new.marital_statuses.widowed'|trans}, patient.marital_status, errors.marital_status ?? null) }}
            </div>
            <div class="ui divider"></div>

            {{ ui.button('patient.edit.save'|trans, 'primary', '', 'save') }}
            <a href="/patients/show?id={{ patient.id }}" class="ui button"><i class="arrow left icon"></i> {{ 'patient.edit.cancel'|trans }}</a>
        </form>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {
            $('.ui.dropdown').dropdown();
        });
    </script>
{% endblock %}
