{% extends "layouts/base.html.twig" %}
{% import "components/ui.html.twig" as ui %}

{% block title %}Склад - Клініка{% endblock %}

{% block body %}
    <div class="ui segment">
        <div class="ui grid">
            <div class="eight wide column">
                <h2 class="ui header">Складські позиції</h2>
            </div>
            <div class="eight wide column right aligned">
                <a href="/inventory/new" class="ui primary button">
                    <i class="plus icon"></i> Додати позицію
                </a>
            </div>
        </div>

        <div class="ui divider"></div>

        {% if lowStockItems is not empty %}
            <div class="ui warning message">
                <div class="header">Увага: Низький рівень запасів!</div>
                <p>Наступні позиції мають кількість нижче мінімального порогу:</p>
                <ul class="list">
                    {% for item in lowStockItems %}
                        <li>{{ item.name }} (Кількість: {{ item.quantity }}, Мін. поріг: {{ item.min_stock_level }})</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        {% if overStockedItems is not empty %}
            <div class="ui info message">
                <div class="header">Інформація: Надлишок запасів!</div>
                <p>Наступні позиції мають кількість вище максимального порогу:</p>
                <ul class="list">
                    {% for item in overStockedItems %}
                        <li>{{ item.name }} (Кількість: {{ item.quantity }}, Макс. поріг: {{ item.max_stock_level }})</li>
                    {% endfor %}
                </ul>
            </div>
        {% endif %}

        <form class="ui form" method="get" action="/inventory">
            <div class="field">
                <div class="ui icon input">
                    <input type="text" name="search" value="{{ searchTerm|default('') }}" placeholder="Пошук позицій...">
                    <i class="search icon"></i>
                </div>
            </div>
        </form>

        <table class="ui celled unstackable table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Назва</th>
                    <th>INN</th>
                    <th>Кількість</th>
                    <th>Мін. рівень запасу</th>
                    <th>Термін придатності</th>
                    <th>Дії</th>
                </tr>
            </thead>
            <tbody>
                {% for item in items %}
                    <tr>
                        <td>{{ item.id }}</td>
                        <td>{{ item.name }}</td>
                        <td>{{ item.inn }}</td>
                        <td>{{ item.quantity }}</td>
                        <td>{{ item.min_stock_level }}</td>
                        <td>{{ item.expiry_date }}</td>
                        <td>
                            <a href="/inventory/show?id={{ item.id }}" class="ui icon button">
                                <i class="eye icon"></i>
                            </a>
                            <a href="/inventory/edit?id={{ item.id }}" class="ui icon button">
                                <i class="edit icon"></i>
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="center aligned">Немає позицій на складі.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
