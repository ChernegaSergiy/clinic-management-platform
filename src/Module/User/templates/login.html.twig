{% extends "layouts/public.html.twig" %}

{% block title %}Вхід — Клініка{% endblock %}

{% block body %}
    <section class="auth">
        <div class="auth__card">
            <p class="eyebrow">Вхід</p>
            <h1 class="auth__title">Увійдіть до облікового запису</h1>
            <form method="POST" action="/login" class="auth__form">
                <label class="auth__field">
                    <span class="auth__label">Email</span>
                    <input class="auth__input" type="email" name="email" value="{{ old.email ?? '' }}" placeholder="you@example.com" required>
                    {% if errors.email %}<div class="auth__error">{{ errors.email }}</div>{% endif %}
                </label>
                <label class="auth__field">
                    <span class="auth__label">Пароль</span>
                    <input class="auth__input" type="password" name="password" placeholder="Ваш пароль" required>
                    {% if errors.password %}<div class="auth__error">{{ errors.password }}</div>{% endif %}
                </label>
                {% if errors.login %}
                    <div class="auth__error-box">
                        <strong>Помилка входу:</strong> {{ errors.login }}
                    </div>
                {% endif %}
                <button type="submit" class="button button--primary button--full">Увійти</button>
            </form>
            <p class="auth__muted">Немає доступу? Зверніться до адміністратора клініки.</p>
        </div>
    </section>
{% endblock %}
