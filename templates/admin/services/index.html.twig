{% extends "layouts/base.html.twig" %}
{% import "components/ui.html.twig" as ui %}

{% block title %}{{ 'admin.services.page_title'|trans }}{% endblock %}

{% block body %}
    <div class="ui segment">
        <div class="ui grid">
            <div class="eight wide column">
                <h2 class="ui header">{{ 'admin.services.title'|trans }}</h2>
            </div>
            <div class="eight wide column right aligned">
                <a href="/admin/services/new" class="ui primary button">
                    <i class="plus icon"></i> {{ 'admin.services.add'|trans }}
                </a>
            </div>
        </div>

        <div class="ui divider"></div>

        <table class="ui celled unstackable table">
            <thead>
                <tr>
                    <th>{{ 'admin.services.table.id'|trans }}</th>
                    <th>{{ 'admin.services.table.name'|trans }}</th>
                    <th>{{ 'admin.services.table.price'|trans }}</th>
                    <th>{{ 'admin.services.table.duration'|trans }}</th>
                    <th>{{ 'admin.services.table.category'|trans }}</th>
                    <th>{{ 'admin.services.table.active'|trans }}</th>
                    <th>{{ 'admin.services.table.actions'|trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for service in services %}
                    <tr>
                        <td>{{ service.id }}</td>
                        <td>{{ service.name }}</td>
                        <td>{{ service.price }}</td>
                        <td>{{ service.duration_minutes }}</td>
                        <td>{{ service.category_name ?? '-' }}</td>
                        <td>
                            {% if service.is_active %}
                                <i class="check icon green"></i> {{ 'admin.services.yes'|trans }}
                            {% else %}
                                <i class="times icon red"></i> {{ 'admin.services.no'|trans }}
                            {% endif %}
                        </td>
                        <td>
                            <a href="/admin/services/edit?id={{ service.id }}" class="ui icon button">
                                <i class="edit icon"></i>
                            </a>
                            <form action="/admin/services/delete" method="POST" style="display: inline;" onsubmit="return confirm('Ви впевнені, що хочете видалити цю послугу?');">
                                <input type="hidden" name="id" value="{{ service.id }}">
                                <button type="submit" class="ui icon button red">
                                    <i class="trash icon"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="7" class="center aligned">Немає послуг для відображення.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
