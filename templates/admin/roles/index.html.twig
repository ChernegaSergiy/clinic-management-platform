{% extends "layouts/base.html.twig" %}
{% import "components/ui.html.twig" as ui %}

{% block title %}{{ 'admin.roles.page_title'|trans }}{% endblock %}

{% block body %}
    <div class="ui segment">
        <div class="ui grid">
            <div class="eight wide column">
                <h2 class="ui header">{{ 'admin.roles.title'|trans }}</h2>
            </div>
            <div class="eight wide column right aligned">
                <!-- Role creation is disabled architecturally -->
            </div>
        </div>

        <div class="ui divider"></div>

        
            <table class="ui celled unstackable table">
                <thead>
                    <tr>
                        <th>{{ 'admin.roles.table.id'|trans }}</th>
                        <th>{{ 'admin.roles.table.name'|trans }}</th>
                        <th>{{ 'admin.roles.table.description'|trans }}</th>
                        <th>{{ 'admin.roles.table.actions'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for role in roles %}
                        <tr>
                            <td>{{ role.id }}</td>
                            <td>{{ role.name }}</td>
                            <td>{{ role.description }}</td>
                            <td>
                                <a href="{{ path('admin_roles_edit_post', {'id': role.id}) }}" class="ui icon button">
                                    <i class="edit icon"></i>
                                </a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="center aligned">{{ 'admin.roles.no_roles'|trans }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
    </div>
{% endblock %}
