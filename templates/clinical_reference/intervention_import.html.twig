{% extends "layouts/base.html.twig" %}

{% block title %}{{ 'clinical_reference.intervention_import.title'|trans }}{% endblock %}

{% block body %}
    <div class="ui segment">
        <h2 class="ui header">{{ 'clinical_reference.intervention_import.header'|trans }}</h2>
        <div class="ui divider"></div>

        {% if errors.import is defined %}
            <div class="ui negative message">{{ errors.import }}</div>
        {% endif %}
        {% if success_message is defined and success_message %}
            <div class="ui positive message">{{ success_message }}</div>
        {% endif %}

        <p>{{ 'clinical_reference.intervention_import.current_count'|trans }} <strong>{{ count }}</strong></p>
        <p>{{ 'clinical_reference.intervention_import.data_source'|trans }} <code>NK-026-2021</code> (meddata.pp.ua) {{ 'clinical_reference.intervention_import.package'|trans }} <code>medcore-ua/nk-026-2021-parser</code>.</p>

        <form class="ui form" method="POST" action="{{ path('clinical_intervention_import_run') }}">
            <button type="submit" class="ui primary button">{{ 'clinical_reference.intervention_import.update_button'|trans }}</button>
            <a href="{{ path('clinical_index') }}" class="ui button">{{ 'clinical_reference.intervention_import.back'|trans }}</a>
        </form>
    </div>
{% endblock %}
