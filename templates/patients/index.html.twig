{% extends "layouts/base.html.twig" %}
{% import "components/ui.html.twig" as ui %}

{% block title %}Пацієнти - Клініка{% endblock %}

{% block body %}
    <div class="ui segment">
        <div class="ui grid">
            <div class="eight wide column">
                <h2 class="ui header">Список пацієнтів</h2>
            </div>
            <div class="eight wide column right aligned">
                {{ ui.button('Додати пацієнта', 'primary', '', 'plus') }}
            </div>
        </div>

        <div class="ui divider"></div>

        <form class="ui form" method="GET" action="/patients">
            <div class="field">
                <div class="ui icon input">
                    <input type="text" name="search" placeholder="Пошук пацієнтів..." value="{{ searchTerm ?? '' }}">
                    <i class="search icon"></i>
                </div>
            </div>
        </form>

        <table class="ui celled table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Ім'я</th>
                    <th>Дата народження</th>
                    <th>Телефон</th>
                    <th>Дії</th>
                </tr>
            </thead>
            <tbody>
                {% for patient in patients %}
                    <tr>
                        <td>{{ patient.id }}</td>
                        <td>{{ patient.name }}</td>
                        <td>{{ patient.birth_date }}</td>
                        <td>{{ patient.phone }}</td>
                        <td>
                            <a href="#" class="ui icon button">
                                <i class="eye icon"></i>
                            </a>
                            <a href="#" class="ui icon button">
                                <i class="edit icon"></i>
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="center aligned">Немає пацієнтів для відображення.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
